# Smart Grid Demo Repository

Synthetic repository used by the VESTA demo runtime.

It contains:
- a small Python HTTP service that acts as the smart-grid control API;
- a JavaScript telemetry helper;
- native snippets in C/C++ so VESTA can detect supported languages;
- a `.vesta/dast.profile.json` file so the profiled DAST flow can run.

